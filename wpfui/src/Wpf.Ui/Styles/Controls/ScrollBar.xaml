<!--
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.
-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:Wpf.Ui.Common"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
    xmlns:converters="clr-namespace:Wpf.Ui.Converters"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">


    <Duration x:Key="ScrollAnimationDuration">0:0:0.22</Duration>
    <Duration x:Key="ButtonHoverAnimationDuration">0:0:0.18</Duration>

    <CubicEase x:Key="EaseOut" EasingMode="EaseOut" />
    <CubicEase x:Key="EaseInOut" EasingMode="EaseInOut" />
    <sys:Double x:Key="LineButtonHeight">12</sys:Double>
    <sys:Double x:Key="LineButtonWidth">12</sys:Double>

    <converters:ObjectToSymbolConverter x:Key="ObjectToSymbolConverter" />

    <Style x:Key="UiScrollBarLineButton" TargetType="RepeatButton">
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource ControlStrongFillColorDefault}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="{StaticResource LineButtonWidth}" />
        <Setter Property="Height" Value="{StaticResource LineButtonHeight}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border
                        x:Name="Border"
                        CornerRadius="6">
                        <Border.Background>
                            <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" Opacity="0" />
                        </Border.Background>

                        <controls:SymbolIcon
                            FontSize="8"
                            Foreground="{TemplateBinding Foreground}"
                            Symbol="{Binding Content, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ObjectToSymbolConverter}}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                            To="1"
                                            Duration="{StaticResource ButtonHoverAnimationDuration}"
                                            EasingFunction="{StaticResource EaseOut}" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                            To="0"
                                            Duration="{StaticResource ButtonHoverAnimationDuration}"
                                            EasingFunction="{StaticResource EaseInOut}" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UiScrollBarPageButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UiScrollBarThumb" TargetType="Thumb">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource ControlStrongFillColorDefault}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Border.CornerRadius" Value="4" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                        Background="{TemplateBinding Background}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        BorderThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="UiVerticalScrollBar" TargetType="ScrollBar">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition MaxHeight="14" />
                <RowDefinition Height="*" />
                <RowDefinition MaxHeight="14" />
            </Grid.RowDefinitions>

            <Border
                x:Name="PART_Border"
                Grid.RowSpan="3"
                Width="12"
                HorizontalAlignment="Center"
                CornerRadius="6">
                <Border.Background>
                    <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" Opacity="0" />
                </Border.Background>
            </Border>

            <RepeatButton
                x:Name="PART_ButtonScrollUp"
                Grid.Row="0"
                Command="ScrollBar.LineUpCommand"
                Content="{x:Static common:SymbolRegular.ChevronUp24}"
                Opacity="0"
                Style="{StaticResource UiScrollBarLineButton}" />

            <Track
                x:Name="PART_Track"
                Grid.Row="1"
                Width="6"
                IsDirectionReversed="True">

                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource UiScrollBarPageButton}" />
                </Track.DecreaseRepeatButton>

                <Track.Thumb>
                    <Thumb Style="{StaticResource UiScrollBarThumb}" />
                </Track.Thumb>

                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource UiScrollBarPageButton}" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton
                x:Name="PART_ButtonScrollDown"
                Grid.Row="2"
                Command="ScrollBar.LineDownCommand"
                Content="{x:Static common:SymbolRegular.ChevronDown24}"
                Opacity="0"
                Style="{StaticResource UiScrollBarLineButton}" />
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="Width"
                                             To="10" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_Border"
                                             Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollUp"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollDown"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>

                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="Width"
                                             To="6" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_Border"
                                             Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollUp"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollDown"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="UiHorizontalScrollBar" TargetType="ScrollBar">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MaxWidth="18" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition MaxWidth="18" />
            </Grid.ColumnDefinitions>

            <Border
                x:Name="PART_Border"
                Grid.ColumnSpan="3"
                Height="12"
                VerticalAlignment="Center"
                CornerRadius="6">
                <Border.Background>
                    <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" Opacity="0" />
                </Border.Background>
            </Border>

            <RepeatButton
                x:Name="PART_ButtonScrollLeft"
                Grid.Column="0"
                Command="ScrollBar.LineLeftCommand"
                Content="{x:Static common:SymbolRegular.ChevronLeft24}"
                Opacity="0"
                Style="{StaticResource UiScrollBarLineButton}" />

            <Track
                x:Name="PART_Track"
                Grid.Column="1"
                Height="6"
                VerticalAlignment="Center">

                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Style="{StaticResource UiScrollBarPageButton}" />
                </Track.DecreaseRepeatButton>

                <Track.Thumb>
                    <Thumb Style="{StaticResource UiScrollBarThumb}" />
                </Track.Thumb>

                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Style="{StaticResource UiScrollBarPageButton}" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton
                x:Name="PART_ButtonScrollRight"
                Grid.Column="2"
                Command="ScrollBar.LineRightCommand"
                Content="{x:Static common:SymbolRegular.ChevronRight24}"
                Opacity="0"
                Style="{StaticResource UiScrollBarLineButton}" />
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="Height"
                                             To="10" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_Border"
                                             Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollLeft"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollRight"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>

                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PART_Track" Storyboard.TargetProperty="Height"
                                             To="6" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_Border"
                                             Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Opacity)"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollLeft"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                            <DoubleAnimation Storyboard.TargetName="PART_ButtonScrollRight"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource ScrollAnimationDuration}"
                                             EasingFunction="{StaticResource EaseInOut}" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="UiScrollBar" TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="OverridesDefaultStyle" Value="True" />

        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="14" />
                <Setter Property="Template" Value="{StaticResource UiVerticalScrollBar}" />
            </Trigger>

            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="14" />
                <Setter Property="Template" Value="{StaticResource UiHorizontalScrollBar}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource UiScrollBar}" TargetType="ScrollBar" />

</ResourceDictionary>
