<ui:UiPage
    x:Class="Voidstrap.UI.Elements.Settings.Pages.IntegrationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Voidstrap.UI.Elements.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Voidstrap.UI.ViewModels"
    xmlns:resources="clr-namespace:Voidstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="IntegrationsPageView"
    Title="IntegrationsPage"
    d:DesignHeight="1580"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">
    <StackPanel Margin="0,0,14,14">
        <TextBlock
            FontSize="14"
            Foreground="{StaticResource TextFillColorSecondaryBrush}"
            Text="{x:Static resources:Strings.Menu_Integrations_Description}" />

        <TextBlock
            Margin="0,8,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="Info" />

        <ui:CardAction Grid.Row="0" Grid.Column="0" Margin="0,8,0,0" Icon="Document24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://voidstrapp.netlify.app/documentation/documentation">
            <StackPanel>
                <TextBlock FontSize="14" Text="Documentation" FontWeight="Medium" />
                <TextBlock Margin="0,2,0,0" FontSize="12" Text="See information about Voidstrap’s features and how to resolve issues." Padding="0,0,16,0"  Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </ui:CardAction>
        
        <ui:CardAction Grid.Row="0" Grid.Column="0" Margin="0,8,0,0" Icon="Globe20" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://voidstrapp.netlify.app">
            <StackPanel>
                <TextBlock FontSize="14" Text="Website" FontWeight="Medium" />
                <TextBlock Margin="0,2,0,0" FontSize="12" Text="View Mods, Crosshairs and more." Padding="0,0,16,0"  Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </ui:CardAction>

        <TextBlock
            Margin="0,8,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="{x:Static resources:Strings.Menu_Integrations_ActivityTracking}" />


        <controls:OptionControl
            x:Name="ActivityTrackingOption"
            Description="{x:Static resources:Strings.Menu_Integrations_EnableActivityTracking_Description}"
            Header="{x:Static resources:Strings.Menu_Integrations_EnableActivityTracking_Title}"
            HelpLink="https://github.com/BloxstrapLabs/Bloxstrap/wiki/What-is-activity-tracking%3F">
            <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding ActivityTrackingEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Description="{Binding Source={x:Static resources:Strings.Menu_Integrations_QueryServerLocation_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://ipinfo.io'}"
            Header="{x:Static resources:Strings.Menu_Integrations_QueryServerLocation_Title}"
            HelpLink="https://github.com/BloxstrapLabs/Bloxstrap/wiki/What-is-activity-tracking%3F#server-location-querying"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding ShowServerDetailsEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
    Description="Displays the game you have joined and its server location in the top-right corner."
    Header="Join-Game Notification"
    IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding joinGameNotify, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Description="{x:Static resources:Strings.Menu_Integrations_PlayerMessageLogs_Description}"
            Header="{x:Static resources:Strings.Menu_Integrations_PlayerMessageLogs_Title}"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding PlayerLogsEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Description="{Binding Source={x:Static resources:Strings.Menu_Integrations_DesktopApp_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://github.com/BloxstrapLabs/Bloxstrap/wiki/What-is-activity-tracking%3F#dont-exit-to-desktop-app'}"
            Header="{x:Static resources:Strings.Menu_Integrations_DesktopApp_Title}"
            HelpLink="https://github.com/BloxstrapLabs/Bloxstrap/wiki/What-is-activity-tracking%3F#dont-exit-to-desktop-app"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding DisableAppPatchEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <Expander Header="Extra Supports" Margin="0,8,0,0">
            <StackPanel Orientation="Vertical">

                <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Mobile support" />

                <ui:CardAction
            Click="OpenCustomEditor_Click"
            Icon="PhoneAdd24" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="Mobile Setup" />
                        <TextBlock
                    Margin="0,2,0,0"
                    Padding="0,0,16,0"
                    FontSize="12"
                    Text="Edit Mobile settings and setup Mobile Support for Windows." />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction
    Click="OpenMobileExplain_Click"
    Icon="ChatHelp20"
    Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="Mobile Setup Explanation" />
                        <TextBlock
            Margin="0,2,0,0"
            Padding="0,0,16,0"
            FontSize="12"
            Text="Learn what Remote Access for Mobile Support does and how to set it up correctly." />
                    </StackPanel>
                </ui:CardAction>

                <TextBlock
            Margin="0,8,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="Discord In-Game Chat" />


                <controls:OptionControl 
    Header="View Discord Chat"
    Description="Chat with others via Discord in-game, with a clearer view.">
                    <Button Content="View" 
            Command="{Binding ChatModeWindowCommand}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"/>
                </controls:OptionControl>

                <controls:OptionControl Header="In-Game Chat Overlay" Description="Displays the Discord Chat Overlay but ingame chat with others. (No Age Verify)">
                    <ui:ToggleSwitch IsChecked="{Binding IngameChatDiscord, Mode=TwoWay}" />
                </controls:OptionControl>

                <ui:CardAction
    Click="OpenDisyChatExplain_Click"
    Icon="ChatHelp20"
    Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="Discord Chat Setup Explanation" />
                        <TextBlock
            Margin="0,2,0,0"
            Padding="0,0,16,0"
            FontSize="12"
            Text="Learn how to setup the chatbot to chat with your friends on Discord." />
                    </StackPanel>
                </ui:CardAction>

                <controls:OptionControl 
    Header="View Music Player"
    Description="View and listen to your music, or manage from the tray menu.">
                    <Button Content="View" 
            Command="{Binding MusicWindowCommand}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"/>
                </controls:OptionControl>

            </StackPanel>
        </Expander>

        <TextBlock
            Margin="0,16,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="{x:Static resources:Strings.Common_DiscordRichPresence}" />
        <controls:MarkdownTextBlock
            Foreground="{StaticResource TextFillColorSecondaryBrush}"
            MarkdownText="{Binding Source={x:Static resources:Strings.Menu_Integrations_RequiresActivityTracking}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://github.com/Bloxstraplabs/Bloxstrap/wiki/What-is-activity-tracking%3F#discord-rich-presence'}"
            TextWrapping="Wrap" />

        <controls:OptionControl
            x:Name="DiscordActivityOption"
            Description="{Binding Source={x:Static resources:Strings.Menu_Integrations_ShowGameActivity_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://github.com/Bloxstraplabs/Bloxstrap/wiki/Discord-Rich-Presence-does-not-work'}"
            Header="{x:Static resources:Strings.Menu_Integrations_ShowGameActivity_Title}"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding DiscordActivityEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Description="{x:Static resources:Strings.Menu_Integrations_ShowAccountOnProfile_Description}"
            Header="{x:Static resources:Strings.Menu_Integrations_ShowAccountOnProfile_Title}"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=DiscordActivityOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding DiscordAccountOnProfile, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Description="{x:Static resources:Strings.Menu_Integrations_AllowActivityJoining_Description}"
            Header="{x:Static resources:Strings.Menu_Integrations_AllowActivityJoining_Title}"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=DiscordActivityOption, Mode=OneWay}">
            <ui:ToggleSwitch Background="{DynamicResource SystemAccentBrush}" IsChecked="{Binding DiscordActivityJoinEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
    Header="Custom RPC"
    Description="Make/Edit a custom RPC for Discord.">
            <Button Content="View" 
            Command="{Binding RPCWindowCommand}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"/>
        </controls:OptionControl>

        <Expander Header="RPC Advanced" Margin="0,8,0,0">
            <StackPanel Orientation="Vertical">
                <controls:OptionControl
            Header="Game Server Location"
            Description="Displays your In Game Server Location on Discord."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding ServerLocationGame, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="Game Name"
            Description="Displays your Roblox Games Name on Discord."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding GameNameChecked, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="Game Creator"
            Description="Displays your Roblox Games Creators Name/Group on Discord."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding GameCreatorChecked, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="Game WIP"
            Description="Displays your Roblox Games In works [BETA],[ALPHA],[TESTING],[PREVIEW]"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding GameWIP, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="Game Icon"
            Description="Displays your Roblox Games Icon on Discord."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding GameIconChecked, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="FFlag Amount"
            Description="Displays your Roblox FFlag Amount on RPC."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding FFlagAmountRPC, Mode=TwoWay}" />
                </controls:OptionControl>
                <controls:OptionControl
            Header="Server Status"
            Description="Displays your Roblox Games Server Status (Public, Private, Reserved)."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:ToggleSwitch
                Background="{DynamicResource SystemAccentBrush}"
                IsChecked="{Binding GameStatusChecked, Mode=TwoWay}" />
                </controls:OptionControl>

                <controls:OptionControl
            Header="Custom Image"
            Description="Displays a custom image instead of the games image."
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:TextBox
                        Width="200"
                        Margin="5,0,0,0"
                        Padding="10,5,10,5"
                        Text="{Binding gameimage, Mode=TwoWay}" />
                </controls:OptionControl>

                <controls:OptionControl
            Header="Custom Game Name"
            Description="Displays a custom Game Name instead of the games name. (Game Name Must be Enabled)"
            IsEnabled="{Binding InnerContent.IsChecked, ElementName=ActivityTrackingOption, Mode=OneWay}">
                    <ui:TextBox
                        Width="200"
                        Margin="5,0,0,0"
                        Padding="10,5,10,5"
                        Text="{Binding gamename, Mode=TwoWay}" />
                </controls:OptionControl>
            </StackPanel>
        </Expander>

        <TextBlock
            Margin="0,16,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="Roblox" />


        <Expander Header="Roblox Settings" IsExpanded="False" Margin="0,8,0,0">
            <StackPanel Orientation="Vertical">
                <controls:OptionControl
            Header="System Tray"
            Description="Would you like to use Roblox System Tray.">
                    <ui:ToggleSwitch IsChecked="{Binding RobloxSystemTray, Mode=TwoWay}" />
                </controls:OptionControl>

                <controls:OptionControl 
            Header="Launch Startup"
            Description="Would you like to Launch Roblox on Startup.">
                    <ui:ToggleSwitch IsChecked="{Binding LaunchStartup, Mode=TwoWay}" />
                </controls:OptionControl>

                <controls:OptionControl 
            Header="Anti AFK"
            Description="Auto rejoins the game instead of getting kicked for being AFK every 20 minutes.">
                    <ui:ToggleSwitch IsChecked="{Binding AntiAFK, Mode=TwoWay}" />
                </controls:OptionControl>
            </StackPanel>
        </Expander>

        <controls:OptionControl 
            Header="FPS Unlocker"
            Description="Removes the 240 FPS Limit without using FastFlags.">
            <ui:ToggleSwitch IsChecked="{Binding UncapFPS, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
    Header="Account Switcher"
    Description="Easily create or switch Roblox accounts. Backup Accounts for Later Use.">
            <StackPanel>
                <Button Content="View"
                HorizontalAlignment="Left"
                Command="{Binding AccountWindowCommand}"/>
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl 
    Header="View Game History"
    Description="View your Roblox game history.">
            <Button Content="View" 
            Command="{Binding OpenHistoryWindowCommand}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"/>
        </controls:OptionControl>


        <TextBlock Text="{x:Static resources:Strings.Menu_Integrations_Custom_Title}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />
        <TextBlock Text="{x:Static resources:Strings.Menu_Integrations_Custom_Description}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <Grid Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListBox x:Name="CustomIntegrationsListBox" Grid.Row="0" Grid.Column="0" Margin="0,0,4,0" ItemsSource="{Binding CustomIntegrations, Mode=OneWay}" DisplayMemberPath="Name" SelectionChanged="CustomIntegrationSelection" SelectedIndex="{Binding SelectedCustomIntegrationIndex, Mode=TwoWay}" />
            <Grid Grid.Row="1" Grid.Column="0" Margin="0,8,4,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Margin="0,0,4,0" Icon="Add28" Content="{x:Static resources:Strings.Common_New}" HorizontalAlignment="Stretch" Command="{Binding AddIntegrationCommand, Mode=OneTime}" />
                <ui:Button Grid.Column="1" Margin="4,0,0,0" Icon="Delete28" Content="{x:Static resources:Strings.Common_Delete}" HorizontalAlignment="Stretch" Appearance="Danger" IsEnabled="{Binding IsCustomIntegrationSelected, Mode=OneWay}" Command="{Binding DeleteIntegrationCommand, Mode=OneTime}" />
            </Grid>
            <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="4,0,0,0">
                <StackPanel.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCustomIntegrationSelected}" Value="False">
                                <Setter Property="StackPanel.Visibility" Value="Hidden"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="{x:Static resources:Strings.Common_Name}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:TextBox Margin="0,4,0,0" Text="{Binding SelectedCustomIntegration.Name, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Margin="0,8,0,0" Text="{x:Static resources:Strings.Menu_Integrations_Custom_AppLocation}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0" Margin="0,0,0,0" PlaceholderText="C:\Windows\System32\cmd.exe" Text="{Binding SelectedCustomIntegration.Location}" />
                    <ui:Button Grid.Column="1" Margin="8,0,0,0" Height="34" Icon="Folder24" Content="{x:Static resources:Strings.Common_Browse}" Command="{Binding BrowseIntegrationLocationCommand}" />
                </Grid>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" FontSize="14" VerticalAlignment="Center" Margin="0,0,4,0" Text="Launch Delay" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ui:TextBox Grid.Column="1" Margin="0,0,16,0" PlaceholderText="200" Text="{Binding SelectedCustomIntegration.Delay}" TextWrapping="NoWrap" AcceptsReturn="True" AcceptsTab="True" />
                    <TextBlock Grid.Column="2" FontSize="14" VerticalAlignment="Center" Margin="0,0,4,0" Text="{x:Static resources:Strings.Menu_Integrations_Custom_LaunchArgs}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ui:TextBox Grid.Column="3" PlaceholderText="{Binding Source='/k echo {0}', Converter={StaticResource StringFormatConverter}, ConverterParameter={x:Static resources:Strings.Menu_Integrations_Custom_LaunchArgs_Placeholder}}" Text="{Binding SelectedCustomIntegration.LaunchArgs}" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" />
                </Grid>
                <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                    <CheckBox Margin="0,0,8,0" Content="{x:Static resources:Strings.Menu_Integrations_Custom_AutoClose}" IsChecked="{Binding SelectedCustomIntegration.AutoClose}" />
                    <CheckBox Margin="0,0,8,0" Content="Pre Roblox Launch" IsChecked="{Binding SelectedCustomIntegration.PreLaunch}" />
                </StackPanel>
            </StackPanel>
            <TextBlock Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Text="{x:Static resources:Strings.Menu_Integrations_Custom_NoneSelected}" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCustomIntegrationSelected}" Value="True">
                                <Setter Property="TextBlock.Visibility" Value="Hidden"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </StackPanel>
</ui:UiPage>
