<ui:UiPage
    x:Class="Voidstrap.UI.Elements.Settings.Pages.ChannelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Voidstrap.UI.Elements.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Voidstrap.UI.Elements.Settings.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Voidstrap.UI.ViewModels.Settings"
    xmlns:resources="clr-namespace:Voidstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Settings"
    d:DataContext="{d:DesignInstance Type=models:ChannelViewModel}"
    d:DesignHeight="2000"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            Margin="0,0,0,8"
            FontSize="14"
            Foreground="{StaticResource TextFillColorSecondaryBrush}"
            Text="{x:Static resources:Strings.Menu_Channel_Description}" />

        <!--  Voidstrap Settings Section  -->
        <TextBlock
            Margin="0,16,0,0"
            FontSize="16"
            FontWeight="Medium"
            Text="Voidstrap" />


        <controls:OptionControl 
    Header="CPU Core Limit"
    Description="Limit the max CPU cores that the app uses.">

            <ComboBox Grid.Column="1" Margin="8,0,8,0" Padding="10,5,10,5" Width="200"
              ItemsSource="{Binding CpuLimitOptions}" 
              SelectedItem="{Binding SelectedCpuLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </controls:OptionControl>

        <!--  Auto Update Toggle  -->
        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_AutoUpdate_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_AutoUpdate_Description}">
            <ui:ToggleSwitch IsChecked="{Binding UpdateCheckingEnabled, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
    Header="Check for Updates"
    Description="Check for any available Voidstrap updates.">
            <StackPanel>
                <Button Content="Check for Updates"
                HorizontalAlignment="Left"
                Click="Check_Click"/>
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl 
    Header="Global Settings"
    Description="Applies your current settings to every bootstrapper installed.">
            <StackPanel>
                <Button Content="Apply"
                HorizontalAlignment="Left"
                Click="ApplyNow_Click"/>
            </StackPanel>
        </controls:OptionControl>

        <Expander Header="App Settings" IsExpanded="False" Margin="0,8,0,0">
            <StackPanel>

                <controls:OptionControl Header="Disable hardware acceleration (WIP)"
    Description="When disabled, Voidstrap won't take up as much memory usage and disables All Animations and Transparency effects. Requires App Restart.">
                    <ui:ToggleSwitch IsChecked="{Binding HWAsselEnabled, Mode=TwoWay}"
                     Checked="ToggleSwitch_Checked_1"
                     Unchecked="ToggleSwitch_Unchecked_1" />
                </controls:OptionControl>

                <controls:OptionControl
            Description="Displays Voidstrap notifications, joined servers, and more."
            Header="App Notifications">
                    <ui:ToggleSwitch IsChecked="{Binding VoidNotify, Mode=TwoWay}" />
                </controls:OptionControl>

                <controls:OptionControl
            Description="Displays Voidstraps discord RPC."
            Header="Voidstrap RPC">
                    <ui:ToggleSwitch IsChecked="{Binding VoidRPC, Mode=TwoWay}" />
                </controls:OptionControl>

            </StackPanel>
        </Expander>

        <TextBlock
            Margin="0,16,0,0"
            FontSize="16"
            FontWeight="Medium"
            Text="Installer" />

        <controls:OptionControl
    Header="BufferSize kilobyte"
    Description="Specify the buffer size in kilobytes.">
            <ui:TextBox
        Width="200"
        Margin="5,0,0,0"
        Padding="10,5,10,5" Text="{Binding BufferSizeKbte, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
    Header="BufferSize kilobytes"
    Description="Enter the desired buffer size in kilobytes.">
            <ui:TextBox
        Width="200"
        Margin="5,0,0,0"
        Padding="10,5,10,5" Text="{Binding BufferSizeKbtes, Mode=TwoWay}" />
        </controls:OptionControl>

        <TextBlock
            Margin="0,16,0,0"
            FontSize="16"
            FontWeight="Medium"
            Text="Roblox" />
        
        <Expander Header="Resolution Settings" IsExpanded="False" Margin="0,8,0,0">
            <StackPanel>

                <controls:OptionControl
    Header="Resolution Changer"
    Description="Select a resolution for your primary monitor">

                    <StackPanel>
                        <ComboBox
            ItemsSource="{Binding AvailableResolutions}"
            SelectedItem="{Binding SelectedResolution, Mode=TwoWay}"
            DisplayMemberPath="DisplayName"
            Width="220"/>
                    </StackPanel>
                </controls:OptionControl>

                <controls:OptionControl
    Header="Resolution Changer (In-Game)"
    Description="Change resolution when entering a specific Roblox PlaceId">

                    <StackPanel>

                        <CheckBox
            Content="Apply Resolution PlaceID"
            IsChecked="{Binding UsePlaceId, Mode=TwoWay}"/>

                        <TextBox
            Width="220"
            Text="{Binding PlaceId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            IsEnabled="{Binding UsePlaceId}"/>

                        <ComboBox
            ItemsSource="{Binding AvailableResolutionsInGame}"
            SelectedItem="{Binding SelectedResolutionInGame, Mode=TwoWay}"
            DisplayMemberPath="DisplayName"
            Width="220"
            Margin="0,8,0,0"/>

                    </StackPanel>
                </controls:OptionControl>

            </StackPanel>
        </Expander>

        <controls:OptionControl 
    Header="Roblox Priority"
    Description="Choose a custom Roblox CPU Priority.">
            <ComboBox Grid.Column="1"
              Margin="8,0,8,0"
              Padding="10,5,10,5"
              Width="200"
              ItemsSource="{Binding PriorityOptions}"
              SelectedItem="{Binding SelectedPriority, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </controls:OptionControl>

        <!--  Force Roblox Reinstallation  -->
        <controls:OptionControl Description="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Description}" Header="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Title}">
            <controls:OptionControl.Style>
                <Style TargetType="controls:OptionControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ForceRobloxReinstallation, Mode=OneTime}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:OptionControl.Style>
            <ui:ToggleSwitch IsChecked="{Binding ForceRobloxReinstallation, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Description="When disabled, Roblox won't update to newer versions." Header="Roblox updates">
            <controls:OptionControl.Style>
                <Style TargetType="controls:OptionControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ForceRobloxReinstallation, Mode=OneTime}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:OptionControl.Style>
            <ui:ToggleSwitch IsChecked="{Binding UpdateRoblox, Mode=TwoWay}" />
        </controls:OptionControl>

        <ui:CardExpander Margin="0,8,0,0" IsExpanded="False">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                        <TextBlock FontSize="14" Text="Channels - [Roblox Build]" />
                        <TextBlock FontSize="12"
                           Text="Enter deployment channel name. Invalid channels will cause 403-404 error!"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           TextWrapping="Wrap" />
                    </StackPanel>

                    <TextBox Grid.Column="1"
                     Width="200"
                     Margin="8,0,8,0"
                     Padding="10,5,10,5"
                     FontSize="13"
                     VerticalContentAlignment="Center"
                     ToolTip="Enter deployment channel (e.g., production, zlive, live)"
                     Text="{Binding ViewChannel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, FallbackValue=production}"
                     MaxLength="2147483647"
                     AcceptsReturn="False"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Disabled"
                     HorizontalScrollBarVisibility="Auto"
                     IsEnabled="{Binding IsChannelEnabled}" />

                    <ui:ToggleSwitch Grid.Column="2"
                      Margin="8,0,10,0"
                      VerticalAlignment="Center"
                      IsChecked="{Binding IsChannelEnabled, Mode=TwoWay}" />
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="IsEnabled" Value="True"/>
                        <Setter Property="Opacity" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChannelEnabled}" Value="False">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <Grid Margin="0,0,4,0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChannelDeployInfo}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,16,8" VerticalAlignment="Center" Text="{x:Static resources:Strings.Common_Version}" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding ChannelDeployInfo.Version, Mode=OneWay}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,16,8" VerticalAlignment="Center" Text="{x:Static resources:Strings.Common_VersionGuid}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding ChannelDeployInfo.VersionGuid, Mode=OneWay}" />

                    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,16,0,0" Orientation="Horizontal">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowChannelWarning, Mode=OneWay}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <Image Grid.Column="0" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" Source="pack://application:,,,/Resources/MessageBox/Warning.png" />
                        <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.Menu_Deployment_Channel_OutOfDate}" />
                    </StackPanel>
                </Grid>

                <Grid Column="0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChannelDeployInfo}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:ProgressRing Grid.Column="0" Margin="6" IsIndeterminate="True">
                        <ui:ProgressRing.Style>
                            <Style TargetType="ui:ProgressRing" BasedOn="{StaticResource {x:Type ui:ProgressRing}}">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowLoadingError, Mode=OneWay}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ui:ProgressRing.Style>
                    </ui:ProgressRing>

                    <Image Grid.Column="0" Margin="6" Width="60" Height="60" Source="pack://application:,,,/Resources/MessageBox/Error.png">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowLoadingError, Mode=OneWay}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>

                    <TextBlock Grid.Column="1" Margin="16" VerticalAlignment="Center" Text="{Binding ChannelInfoLoadingText, Mode=OneWay}" TextWrapping="Wrap" />
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Channels"
            Click="OpenChannelListDialog_Click" />
                </StackPanel>
            </StackPanel>
        </ui:CardExpander>

        <controls:OptionControl x:Name="RobloxVersionAPP" Header="">
        </controls:OptionControl>
        <StackPanel Orientation="Horizontal">
            <Button Content="Reset Settings"
            Margin="0,8,8,0"
            Click="Button_Click" />

            <ui:Button Content="Donate"
               Icon="Heart24"
               Margin="0,8,8,0"
               Click="DonateButton_Click" />
        </StackPanel>
    </StackPanel>
</ui:UiPage>
